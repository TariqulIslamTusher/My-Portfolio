import React from 'react';
import SectionTitle from '../../Hooks/SectionTitle';

const MyProjects = () => {
    return (
        <div>
            <SectionTitle heading={'Projects'} subHeading={'All the rated projects of mine'}></SectionTitle>
        </div>
    );
};

export default MyProjects;